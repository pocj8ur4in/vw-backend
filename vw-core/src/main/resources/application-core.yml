
---
# 개발 (dev) 프로파일
spring:
  config:
    activate:
      on-profile: dev

---
# 스테이징 (staging) 프로파일
spring:
  config:
    activate:
      on-profile: staging

---
# 운영 (production) 프로파일
spring:
  config:
    activate:
      on-profile: production
